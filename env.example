# H2 Configuration (for local development and tests)
DB_URL=jdbc:h2:mem:testdb
DB_USERNAME=sa
DB_PASSWORD=password

# MySQL Docker Configuration (for docker)
MYSQL_DATABASE=skyroute
MYSQL_USER=skyroute
MYSQL_PASSWORD=skyroute_pwd
MYSQL_ROOT_PASSWORD=root_pwd

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-here-make-it-very-long-and-secure
JWT_EXPIRATION=3600000
JWT_REFRESH_EXPIRATION=86400000

# Mail Configuration
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=skyroute.dreamteam@gmail.com
MAIL_PASSWORD=your-email-password
MAIL_PROPERTIES_MAIL_SMTP_AUTH=true
MAIL_PROPERTIES_MAIL_SMTP_STARTTLS_ENABLE=true
MAIL_PROPERTIES_MAIL_SMTP_STARTTLS_REQUIRED=true

# Cloudinary Configuration
CLOUDINARY_CLOUD_NAME=your-cloudinary-cloud-name
CLOUDINARY_API_KEY=your-cloudinary-api-key
CLOUDINARY_API_SECRET=your-cloudinary-api-secret

# Application Configuration
SPRING_PROFILES_ACTIVE=local

# CORS Configuration
# Comma-separated list of allowed origins for CORS
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8080,http://localhost:5173

# Scheduler Configuration
APP_SCHEDULER_ENABLED=true
APP_SCHEDULER_FLIGHT_AVAILABILITY_CRON=0 0 2 * * ?